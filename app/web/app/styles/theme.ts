export const themeTokens = {
  color: {
    background: "var(--revela-background)",
    surface: "var(--revela-surface)",
    surfaceRaised: "var(--revela-surface-raised)",
    text: "var(--revela-text)",
    textMuted: "var(--revela-text-muted)",
    accent: "var(--revela-accent)",
    border: "var(--revela-border)",
  },
  radius: {
    small: "var(--revela-radius-sm)",
    medium: "var(--revela-radius-md)",
    large: "var(--revela-radius-lg)",
  },
  shadow: {
    panel: "var(--revela-shadow-panel)",
  },
} as const;
